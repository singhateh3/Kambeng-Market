# render.yaml

services:
  - type: web
    name: kambeng-market-api
    runtime: php
    plan: free
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      php artisan config:cache
      php artisan route:cache
      php artisan view:cache
      php artisan migrate --force
    startCommand: php artisan serve --host=0.0.0.0 --port=10000
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
      - key: APP_URL
        value: https://kambeng-market-api.onrender.com
      - key: FRONTEND_URL
        value: https://kambeng-market-frontend.onrender.com
      - key: DB_CONNECTION
        value: mysql
      - key: DB_HOST
        fromDatabase:
          name: kambeng-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: kambeng-db
          property: port
      - key: DB_DATABASE
        fromDatabase:
          name: kambeng-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: kambeng-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: kambeng-db
          property: password
      - key: SESSION_DRIVER
        value: database
      - key: CACHE_STORE
        value: database
      - key: QUEUE_CONNECTION
        value: database

  - type: web
    name: kambeng-db
    runtime: mysql
    plan: free
    databaseName: kambeng_market
    user: kambeng_user